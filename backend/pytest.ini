[pytest]
# Run from backend/ ; `pythonpath = .` puts the backend package root on sys.path so
# tests can import main, setups_board, trade_plans_router, scanners.*, etc.
testpaths = tests
pythonpath = .
addopts = -q
